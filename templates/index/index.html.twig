{% extends 'base.html.twig' %}

{% block title %} | Index{% endblock %}

{% block body %}
<section class="container index">
    <div class="row">
        <a class="btn btn-primary" href="{{ path('noticia') }}"><span>noticias</span></a>
        <a class="btn btn-primary" href="{{ path('socio') }}"><span>socios</span></a>
        <a class="btn btn-primary" href="{{ path('reserva') }}"><span>reservas</span></a>
        <a class="btn btn-primary" href="{{ path('producto') }}"><span>productos</span></a>
        <a class="btn btn-primary" href="{{ path('proveedor') }}"><span>proveedores</span></a>
        <a class="btn btn-primary" href="#"><span>gastos</span></a>
        <a class="btn btn-primary" href="{{ path('ingresos') }}"><span>ingresos</span></a>
        <a class="btn btn-primary" href="#"><span>pedidos</span></a>
    </div>
</section>
{% endblock %}