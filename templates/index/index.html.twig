{% extends 'base.html.twig' %}

{% block title %} | Index{% endblock %}

{% block body %}
<div class="container index">
    <div class="row">
        <a class="btn btn-primary rojo" href="{{ path('noticia') }}">noticias</a>
        <a class="btn btn-primary morado" href="{{ path('socio') }}">socios</a>
        <a class="btn btn-primary azul" href="{{ path('reserva') }}">reservas</a>
        <a class="btn btn-primary verde" href="{{ path('producto') }}">productos</a>
        <a class="btn btn-primary verde-oscuro" href="{{ path('proveedor') }}">proveedores</a>
        <a class="btn btn-primary amarillo" href="#">gastos</a>
        <a class="btn btn-primary naranja" href="{{ path('ingresos') }}">ingresos</a>
        <a class="btn btn-primary gris" href="#">pedidos</a>
    </div>
</div>
{% endblock %}